'use client';

export function BestNodeBadge() {
  return (
    <div className="absolute -top-3 -right-3 bg-gradient-to-r from-status-healthy to-sui-teal text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg animate-pulse-fast">
      ⭐ BEST NODE
    </div>
  );
}